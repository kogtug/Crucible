{
  "name": "@crucible/fixture-basic-server",
  "version": "0.1.0",
  "description": "A minimal, spec-conformant MCP server exposing one tool. Crucible's reference target for the classic initialize-based protocol.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  }
}
