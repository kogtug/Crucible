{
  "name": "@crucible/cli",
  "version": "0.1.0",
  "description": "Command-line interface for Crucible: `crucible scan -- <command>` for conformance checks, `crucible chaos -- <command>` for fault-injection resilience scoring.",
  "type": "module",
  "bin": {
    "crucible": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@crucible/chaos": "*",
    "@crucible/conformance": "*",
    "@crucible/core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@crucible/fixture-basic-server": "*",
    "@crucible/fixture-stateless-server": "*"
  }
}
